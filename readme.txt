cors 
=============

cors hame help kerta hai eak domain se dosre domain ko connect kerne me 
jese localhost:3030 ko use kerte we hum localhost:3031 se connect nhi ho skte
is ke leye hum cors use kerte jo k humme is error se bachata hai

XMLHttpRequest cannot load http://localhost:3000. No 'Access-Control-Allow-Origin' header is present on the requested resource. Origin 'null' is therefore not allowed access.


 